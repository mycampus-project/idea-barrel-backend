import express from "express";

const router = express.Router();

router.get("/", (req, res) => {

});


router.post("/", (req, res) => {
    
});


router.put("/", (req, res) => {
    // isAuthorized
});


router.delete("/", (req, res) => {
    // isAuithorized
});

export default router;